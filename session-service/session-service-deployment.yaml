apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: embb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: session-service
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
      - name: session-service
        image: eliasandronikou/session-service:latest
        env:
        - name: CPU_MULTIPLIER
          value: "1000"
        - name: RAM_MULTIPLIER
          value: "1000000"
        - name: SLICE_TYPE
          value: "embb"  # Slice type for eMBB
        ports:
        - containerPort: 8002
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
        resources:
          requests:
            cpu: "100m" # Updated from 150m
            memory: "128Mi" # No change
          limits:
            cpu: "200m" # Updated from 300m
            memory: "256Mi" # No change
---
apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: embb
  labels:
    app: session-service
spec:
  selector:
    app: session-service
  ports:
  - name: http
    protocol: TCP
    port: 8002
    targetPort: 8002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: massive-iot
spec:
  replicas: 1
  selector:
    matchLabels:
      app: session-service
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
      - name: session-service
        image: eliasandronikou/session-service:latest
        env:
        - name: CPU_MULTIPLIER
          value: "1000"
        - name: RAM_MULTIPLIER
          value: "1000000"
        - name: SLICE_TYPE
          value: "massive-iot"  # Slice type for Massive IoT
        ports:
        - containerPort: 8002
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
        resources:
          requests:
            cpu: "100m" # No change
            memory: "128Mi" # Updated from 192Mi
          limits:
            cpu: "200m" # No change
            memory: "256Mi" # Updated from 384Mi
---
apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: massive-iot
  labels:
    app: session-service
spec:
  selector:
    app: session-service
  ports:
  - name: http
    protocol: TCP
    port: 8002
    targetPort: 8002
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-service
  namespace: urllc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: session-service
  template:
    metadata:
      labels:
        app: session-service
    spec:
      containers:
      - name: session-service
        image: eliasandronikou/session-service:latest
        env:
        - name: CPU_MULTIPLIER
          value: "1000"
        - name: RAM_MULTIPLIER
          value: "1000000"
        - name: SLICE_TYPE
          value: "urllc"  
        ports:
        - containerPort: 8002
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
        resources:
          requests:
            cpu: "200m" 
            memory: "256Mi" 
          limits:
            cpu: "200m"
            memory: "256Mi" 
---
apiVersion: v1
kind: Service
metadata:
  name: session-service
  namespace: urllc
  labels:
    app: session-service
spec:
  selector:
    app: session-service
  ports:
  - name: http
    protocol: TCP
    port: 8002
    targetPort: 8002